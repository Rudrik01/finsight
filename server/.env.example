PORT=5000
NODE_ENV=development
# Client URL isn't strictly required dynamically for CORS anymore as our corsMiddleware allows generic vercel domains. 
# But for strict locking in production, set this:
CORS_ORIGIN=https://your-client-app.vercel.app
